CREATE TABLE `beealive`.`data_short` (
  `timestamp` SMALLINT(5) NOT NULL,
  `microphone` INT NULL,
  PRIMARY KEY (`timestamp`));

CREATE TABLE `beealive`.`data_long` (
  `id` INT NOT NULL,
  PRIMARY KEY (`id`));

